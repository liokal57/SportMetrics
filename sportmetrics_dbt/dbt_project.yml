name: 'sportmetrics'
version: '1.0.0'
config-version: 2

profile: 'sportmetrics'

model-paths: ["sportmetrics"]

analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  sportmetrics:
    +materialized: view
